{
  "name": "@bip/worker",
  "version": "0.1.0",
  "private": true,
  "description": "BullMQ worker process. Idle in Phase 1, activated in Phase 3.",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "node dist/index.js",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^20.12.7",
    "tsx": "^4.7.2",
    "typescript": "^5.4.5"
  }
}
